# --- Web framework ---
fastapi==0.115.5
uvicorn[standard]==0.32.1

# --- Data / ORM ---
sqlalchemy==2.0.36
pydantic==2.10.3
pydantic-settings==2.6.1
# Postgres driver — used in production (Render) for persistent storage; SQLite
# (the default) needs no driver. Harmless when DATABASE_URL is sqlite.
psycopg2-binary==2.9.10

# --- Git ingestion ---
GitPython==3.1.43

# --- Multi-language parsing (tree-sitter) ---
# Prebuilt, compiler-free wheels bundling grammars for JS/TS, Java, Go, Rust,
# Ruby, C/C++, C#, PHP, and more. Python itself is parsed with the stdlib `ast`.
tree-sitter==0.25.2
tree-sitter-language-pack==0.13.0

# --- HTTP client for OpenRouter ---
httpx==0.28.1

# --- RAG / vector store ---
chromadb==0.5.23

# --- Utilities ---
python-dotenv==1.0.1
python-multipart==0.0.18
